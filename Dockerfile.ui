# Stage 1: Builder
FROM python:3.13-slim AS builder

WORKDIR /app

# Install uv for fast dependency management
COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

# Copy dependency files
COPY pyproject.toml uv.lock* README.md ./

# Create virtual environment and install demo dependencies only
RUN uv venv /app/.venv && \
    uv sync --frozen --no-dev --extra demo

# Stage 2: Runtime
FROM python:3.13-slim

WORKDIR /app

# Create non-root user for security
RUN useradd --create-home --shell /bin/bash appuser

# Copy virtual environment from builder
COPY --chown=appuser:appuser --from=builder /app/.venv /app/.venv

# Copy demo application
COPY --chown=appuser:appuser demo/ ./demo/

# Set environment variables
ENV PATH="/app/.venv/bin:$PATH"
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
ENV API_URL=http://api:8000
ENV PYTHONPATH=/app

# Switch to non-root user
USER appuser

# Expose Streamlit port
EXPOSE 8501

# Run Streamlit
CMD ["streamlit", "run", "demo/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
